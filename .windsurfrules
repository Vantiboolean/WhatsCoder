# 强制规则（必须无条件遵守，不可违反或忽略）

## 输出规则

你被严格禁止在聊天窗口中输出任何文字内容。所有输出必须通过 write_to_file 工具写入指定目录。

### 输出目录

`/tmp/ws-output/whats-coder/`

### 文件命名

递增编号：`001.md`、`002.md`、`003.md`...

每次调用 write_to_file 必须使用下一个递增编号。首次回复用 `001.md`，第二次用 `002.md`，依此类推。自行维护计数。

### 输出流程

1. 收到用户消息后，write_to_file 写入下一个编号的 .md 文件到输出目录
2. 文件内容使用标准 Markdown 格式
3. 完成后聊天中只输出：`done`
4. 创建/修改项目代码文件时，正常使用 write_to_file 写入对应路径
5. 如果一次任务需要多个步骤，每完成一个关键步骤就写入一个新编号的 .md 文件

### 文件内容格式

\`\`\`markdown
## 思考过程

（你的分析和推理过程）

## 回复

（正式回复内容，Markdown 格式）
\`\`\`

如果有代码需要展示，直接在回复中用代码块即可。实际项目文件仍用 write_to_file 写入对应路径。

### 严格禁止

- 绝对禁止使用 `edit`、`multi_edit` 或任何差异替换工具！修改已有文件时，必须使用 `write_to_file` 直接覆盖整个文件的全部内容！
- 禁止在聊天窗口输出代码块
- 禁止在聊天窗口输出解释说明
- 禁止在聊天窗口输出思考过程
- 禁止在聊天窗口输出任何超过 10 个字符的内容

### 允许

- 聊天中输出 `done` 表示完成
- 聊天中输出 `working` 表示处理中
- 正常使用内置工具（仅限 write_to_file、grep_search、run_command 等，禁止 edit）
